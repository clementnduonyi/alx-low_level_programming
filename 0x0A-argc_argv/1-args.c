#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the number of arguments passed into it
 * @argc: argument count
 * @argc: array of argument
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	int i;
	if (argv)
	for (i = 1; i <= argc; i++)
	{}
	printf("%d\n", i - 2);

	return (0);
}	
